---
usage: opensource
includes:
- lib
integration:
  name: rspec
requires:
- unparser
mutation:
  timeout: 10.0
coverage_criteria:
  timeout: true
  process_abort: true
matcher:
  subjects:
  - 'Unparser*'
